name: flutterando_metrics_plugin_loader
description: This pubspec determines the version of the analyzer plugin to load.
version: 5.7.6
publish_to: none

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutterando_metrics:
    path: ../../

dev_dependencies:
  lints: ^1.0.1
