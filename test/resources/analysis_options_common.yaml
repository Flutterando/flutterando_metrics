include: package:lints/recommended.yaml

analyzer:
  exclude:
    - example/**
  strong-mode:
    implicit-casts: false
    implicit-dynamic: false

flutterando_metrics:
  metrics-exclude:
    - test/**
  rules:
    - rule-id4
    - rule-id5
